import { serverSupabaseServiceRole, serverSupabaseUser } from '#supabase/server'
import { extractUserId } from '@@/lib/auth'
import type { Database } from '@@/types/database'
export default defineEventHandler(async (event) => { const parentId = extractUserId(await serverSupabaseUser(event)); if (!parentId) throw createError({ statusCode: 401, statusMessage: 'You must be signed in.' }); const adminClient = serverSupabaseServiceRole<Database>(event); const { data: profile } = await adminClient.from('profiles').select('id').eq('id', parentId).eq('role', 'parent').eq('status', 'active').maybeSingle(); if (!profile) throw createError({ statusCode: 403, statusMessage: 'Parent access is required.' }); const { data: links } = await adminClient.from('player_parents').select('player_id').eq('parent_id', parentId); const playerIds = (links ?? []).map((item) => item.player_id); if (!playerIds.length) return { games: [] }; const [{ data: records, error: recordsError }, { data: players }, { data: games }] = await Promise.all([adminClient.from('game_players').select('game_id, player_id, availability_status, availability_note').in('player_id', playerIds), adminClient.from('players').select('id, full_name').in('id', playerIds), adminClient.from('games').select('id, opponent_name, scheduled_at, location_type, status').in('status', ['scheduled', 'postponed']).order('scheduled_at')]); if (recordsError) throw createError({ statusCode: 500, statusMessage: 'Unable to load attendance.' }); const playerMap = new Map((players ?? []).map((item) => [item.id, item])); const gameMap = new Map((games ?? []).map((item) => [item.id, item])); return { games: (records ?? []).flatMap((record) => { const player = playerMap.get(record.player_id); const game = gameMap.get(record.game_id); return player && game ? [{ ...record, player, game }] : [] }) } })
